exports.html = local('controllers/modules/response/html');
exports.json = local('controllers/modules/response/json');

// These functions might be deleted.
exports.redirect = function (data, req, res, next) {
	// Respond with a redirect
	return res.redirect('/api'); // Could POST /api be replaced with data.redirect? If so, I probably won't delete this function.
}

// exports.err = function (query, req, res, next) {
// 	// Respond with an error
// 	return res.json(err);
// };
